# 屏幕契约 (Screen Contract)

> **模板说明**：复制到 `prototypes/<project_code>/specs/` 后，将标题中的占位符替换为真实页面名；删除本提示块。

## 元数据

| 字段 | 内容 |
|------|------|
| 页面 slug | `<page-slug，如 calendar>` |
| 原型文件 | `<如 ../06-calendar.html>` |
| 迭代 / 版本 | `<如 2026-Q2 / v1.2>` |
| 负责人 | `<姓名>` |
| req-specs 基线 | `<commit 短哈希或 tag；例 abc1234 或 specs-v1.0>` |
| 关联 req-specs | `<相对主仓路径，如 req-specs/02-models/domains/.../某文件.md；可多行>` |
| 关联 DDR | `<可选：../design-decisions/06-calendar-ddr.md>` |
| 关联状态清单 | `<同目录：06-calendar-page-states.md>` |

## 1. 页面目标与用户任务

- **本页要完成的任务**：`<一句话>`
- **主用户角色**：`<角色 A；若多角色见权限矩阵>`
- **入口 / 出口**：`<从哪些页来、主要跳转到哪些页>`

## 2. 展示数据范围

### 2.1 数据主体与来源

- **领域对象 / API 资源**：`<名称>`
- **读模型说明**：`<列表 / 详情 / 聚合；是否只读快照>`

### 2.2 过滤、排序与时间窗

| 项目 | 规则 |
|------|------|
| 默认筛选 | `<例如：当前用户、当前照护组、今日>` |
| 排序 | `<字段 + 升降序；默认>` |
| 时间窗 / 时区 | `<例如：按用户设备时区展示；截止日以服务端为准>` |
| 数量上限 | `<列表最大条数；超出时 UI 行为>` |

### 2.3 空数据与无权限

| 场景 | 区分方式 | 界面表现（文案 / 行动点） |
|------|----------|----------------------------|
| 无数据 | `<判断条件>` | |
| 无查看权限 | `<判断条件>` | |
| 部分可见 | `<判断条件>` | |

## 3. 控件状态与控制逻辑

> 涉及枚举、阈值、时间与 `req-specs` 中定义一致的，**写交叉引用**；此处写界面层补充条件。

### 3.1 决策表（标签 / 按钮 / 徽章）

| 控件 ID / 文案 | 显示条件（字段 / 运算符 / 值） | 样式变体 | 可交互 | 点击 / 提交行为 |
|----------------|----------------------------------|----------|--------|------------------|
| `<primary-action>` | | | 是 / 否 | |
| `<status-badge>` | | | — | — |

### 3.2 与后端状态不一致时的表现

- **乐观更新**：`<是 / 否；回滚策略>`
- **轮询 / 推送**：`<无 / 有；频率或事件>`

## 4. 与其他页面 / 模块的依赖

- **必须已具备的前置数据**：`<无 / 列出>`
- **共用组件行为对齐**：`<引用其他屏幕契约路径或写「同 XX 页第 3 节」>`

## 5. 非目标、假设与开放问题

| 主题 | 本期是否包含 | 说明 |
|------|----------------|------|
| 国际化文案长度 | | |
| 无障碍 / 大屏适配 | | |
| 离线 / 弱网 | | |
| 其他 | | |

**开放问题**（须闭环或升产品 / 研发）：

1. `<问题描述 + 责任人 + 目标日期>`

## 6. 变更记录

| 日期 | 作者 | 摘要 |
|------|------|------|
| YYYY-MM-DD | | 初稿 |
